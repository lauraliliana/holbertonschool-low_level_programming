#include "holberton.h"

/**
 * print_line - prints a line as big a n.
 * @n: number of lines
 * Return: void
 */
void print_line(int n)
{
  int i;
  for (i = 0; i < n; i++)
    _putchar(95);
  _putchar(10);
}
