/**
 * _isuppers - virifies if a character is lowercase
 * left in the year, taking leap years into account
 * @c: character to check
 * Return: void
 */
int _isupper(int c)
{
while (c >= 'A' && c <= 'Z')
return (1);
return (0);
}
