#include "holberton.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string.
 * @s: string to measure
 * Return: Always 0.
 */
int _strlen(char *s)
{
int i;
for (i = 0; i < '\0'; i++)
  {

  }
}
